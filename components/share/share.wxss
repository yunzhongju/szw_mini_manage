/* components/share/share.wxss */
.content{
  position: fixed;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: rgba(0, 0, 0, .4);
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
}
.save-btn {
	width: 630rpx;
	margin-top: 20rpx;
	border-radius: 20rpx;
	font-size: 35rpx;
	color: #fff;
	background: rgba(255, 63, 63);
	height: 85rpx;
	line-height: 85rpx;
	text-align: center;
}